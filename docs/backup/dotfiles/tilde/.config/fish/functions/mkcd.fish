# ============================================================================
# 创建目录并进入函数
# ============================================================================
# 创建目录（支持多级目录）并立即进入该目录
# 用法: mkcd <目录路径>

function mkcd --description "创建目录并进入该目录"
    # 检查是否提供了参数
    if test (count $argv) -eq 0
        echo "用法: mkcd <目录路径>"
        return 1
    end

    # 创建目录（支持多级目录）
    if mkdir -p $argv[1]
        # 创建成功后进入目录
        cd $argv[1]
        echo "创建并进入目录: "(pwd)
    else
        echo "错误: 无法创建目录 '$argv[1]'"
        return 1
    end
end
