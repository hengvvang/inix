# ============================================================================
# 快捷命令参考函数
# ============================================================================
# 使用 cheat.sh 服务查询命令用法
# 用法: cheat <命令名称>

function cheat --description "查询命令用法和示例"
    # 检查是否提供了参数
    if test (count $argv) -eq 0
        echo "用法: cheat <命令名称>"
        echo "示例: cheat tar"
        return 1
    end

    # 查询命令参考
    curl -s "cheat.sh/$argv[1]"
end
