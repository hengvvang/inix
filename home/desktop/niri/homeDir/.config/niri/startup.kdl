// =============================================================================
// niri 启动服务配置
// =============================================================================
// 此文件定义 niri 启动时自动运行的服务和程序
//
// 语法说明:
// - spawn-at-startup "程序名" "参数1" "参数2" ...  // 启动单个程序
// - spawn-sh-at-startup "shell命令"                 // 使用shell执行复杂命令
// =============================================================================

// =============================================================================
// 核心系统服务
// =============================================================================

// 输入法框架 - Fcitx5 守护进程
// -d 参数表示以守护进程(daemon)模式运行
spawn-sh-at-startup "fcitx5 -d"

// 通知服务 - Dunst
// 用于显示桌面通知消息
spawn-at-startup "dunst"

// 空闲管理服务 - Swayidle
// 管理系统空闲时的行为（如自动锁屏、休眠等）
spawn-at-startup "swayidle"

// =============================================================================
// 界面相关服务
// =============================================================================

// 状态栏 - Waybar
// 显示系统状态、时间、网络等信息
spawn-at-startup "waybar"

// 备选状态栏 - Ironbar (已禁用)
// 如需使用 ironbar 替代 waybar，请取消下面的注释并注释掉上面的 waybar
// spawn-at-startup "ironbar"

// =============================================================================
// 壁纸服务
// =============================================================================

// 壁纸守护进程 - swww-daemon
// 用于动态壁纸切换和管理
spawn-at-startup "swww-daemon"

// 备选壁纸服务 - Swaybg (已禁用)
// 如需使用 swaybg 替代 swww，请取消下面的注释并注释掉上面的 swww-daemon
// spawn-at-startup "swaybg"

// =============================================================================
// 其他工具
// =============================================================================

// Niri 窗口切换器
spawn-at-startup "niriswitcher"

// 日落色温调节工具
spawn-at-startup "sunsetr"

// =============================================================================
// 延迟启动示例 (已禁用)
// =============================================================================

// 示例: 延迟 5 秒后启动 Firefox
// spawn-sh-at-startup "sleep 5 && firefox"

// 示例: 启动 Gammastep 并设置色温为 4500K
// spawn-at-startup "gammastep" "-O" "4500"
