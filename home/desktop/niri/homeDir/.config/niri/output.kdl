// =============================================================================
// niri 显示输出配置
// =============================================================================
// 此文件定义显示器的分辨率、刷新率、缩放等设置
// 官方文档: https://github.com/YaLTeR/niri/wiki/Configuration:-Outputs
//
// 提示: 运行 `niri msg outputs` 可以查看所有可用的输出设备和模式
// =============================================================================

// =============================================================================
// eDP-1 输出配置 (内置笔记本屏幕)
// =============================================================================
output "eDP-1" {
    // 禁用此输出
    // 取消下面的注释可完全关闭此显示器
    // off

    // ==========================================================================
    // 显示模式设置
    // ==========================================================================

    // 设置显示分辨率和刷新率
    // 格式: "<宽度>x<高度>@<刷新率>"
    //
    // 当前设置: 2560x1600 分辨率，240.003Hz 刷新率
    //
    // 查看所有可用模式:
    // 在 niri 实例内运行: niri msg outputs
    mode "2560x1600@240.003"

    // ==========================================================================
    // 缩放设置
    // ==========================================================================

    // 显示缩放因子
    // - 整数缩放: 1, 2, 3 等
    // - 小数缩放: 1.5 (150%), 1.25 (125%), 2.5 (250%) 等
    //
    // 当前设置: 1.5 = 150% 缩放
    // 适合高分辨率屏幕，使界面元素更易读
    scale 1.5

    // ==========================================================================
    // 屏幕旋转设置
    // ==========================================================================

    // 旋转/翻转显示输出
    // 可用值:
    // - "normal"      : 正常方向 (0°)
    // - "90"          : 逆时针旋转 90°
    // - "180"         : 逆时针旋转 180° (倒置)
    // - "270"         : 逆时针旋转 270°
    // - "flipped"     : 水平翻转
    // - "flipped-90"  : 水平翻转 + 逆时针旋转 90°
    // - "flipped-180" : 水平翻转 + 逆时针旋转 180°
    // - "flipped-270" : 水平翻转 + 逆时针旋转 270°
    transform "normal"

    // ==========================================================================
    // 位置设置 (已禁用)
    // ==========================================================================

    // 手动设置显示器在虚拟屏幕空间中的位置
    // 格式: position x=<X坐标> y=<Y坐标>
    //
    // 注意: 建议让 niri 自动处理输出位置，避免潜在的渲染问题
    // 仅在多显示器设置需要特定布局时才手动设置
    //
    // 示例: 将此显示器放在坐标 (1280, 0)
    // position x=1280 y=0
}

// =============================================================================
// 多显示器配置示例 (已禁用)
// =============================================================================

// HDMI-A-1 外接显示器示例配置
// 取消下面的注释以配置外接显示器
/*
output "HDMI-A-1" {
    // 4K 分辨率，60Hz 刷新率
    mode "3840x2160@60"

    // 2倍缩放，使 4K 显示器上的界面更易读
    scale 2.0

    // 正常方向
    transform "normal"

    // 位置: 放在主显示器右侧
    // 假设主显示器(eDP-1)宽度为 2560，缩放1.5后逻辑宽度约为 1707
    // position x=1707 y=0
}
*/

// DisplayPort 外接显示器示例配置
/*
output "DP-1" {
    // 2K 分辨率，144Hz 高刷新率（适合游戏）
    mode "2560x1440@144"

    // 无缩放
    scale 1.0

    // 正常方向
    transform "normal"
}
*/

// 竖屏显示器示例配置
/*
output "DP-2" {
    // 1080p 分辨率
    mode "1920x1080@60"

    // 无缩放
    scale 1.0

    // 旋转90度，竖屏使用（适合阅读代码或文档）
    transform "90"
}
*/
